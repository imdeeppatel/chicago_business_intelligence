steps:
# Steps to pull a docker image for pgadmin, push it to container registry and deploy it to cloud run.
- name: "gcr.io/cloud-builders/docker"
  args: ['pull', 'dpage/pgadmin4']
- name: "gcr.io/cloud-builders/docker"
  args: ['tag', 'dpage/pgadmin4','PROJECT-ID/pgadmin']
- name: "gcr.io/cloud-builders/docker"
  args: ['push', 'PROJECT-ID']
#deply pg-admin
- name: "gcr.io/google.com/cloudsdktool/cloud-sdk"
  entrypoint: gcloud
  args: ['run', 'deploy','pg-admin', '--image','PROJECT-ID', '--region','us-central1', '--add-cloudsql-instances', 'atomic-airship-404501:us-central1:mypostgres','--platform','managed', '--port','80', '--allow-unauthenticated','--set-env-vars','PGADMIN_DEFAULT_EMAIL=user@gmail.com','--set-env-vars','PGADMIN_DEFAULT_PASSWORD=SuperSecret']
  
# Steps to build a docker image for go-microservice, push it to container registry and deploy it to cloud run.  
- name: "gcr.io/cloud-builders/docker"
  args: ['build', '-t', 'PROJECT-ID/go-microservice','.']
- name: "gcr.io/cloud-builders/docker"
  args: ['push', 'PROJECT-ID/go-microservice']
  
- name: "gcr.io/google.com/cloudsdktool/cloud-sdk"
  entrypoint: gcloud
  args: ['run', 'deploy','go-microservice', '--image','PROJECT-ID/go-microservice', '--region','us-central1', '--add-cloudsql-instances', 'atomic-airship-404501:us-central1:mypostgres','--platform','managed', '--port','8080', '--allow-unauthenticated']
images:
- PROJECT-ID/go-microservice
- PROJECT-ID/pgadmin